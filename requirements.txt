# Everything here is free and open source.
# The only "engine" is Ollama, installed separately (see SETUP.md).

langchain>=0.3
langchain-community>=0.3
langchain-core>=0.3
langchain-ollama>=0.2      # local LLM + embeddings
langchain-chroma>=0.1      # free local vector store
langchain-text-splitters>=0.3
langgraph>=0.2             # the agent framework (step 6)
ragas>=0.2                 # free eval library (step 7); runs on local models
langsmith>=0.1             # tracing client; free Developer tier
python-dotenv>=1.0         # loads your .env
