# prompt: judge_correctness
# version: 1.0
# used_by: src/eval_langsmith.py → _CORRECTNESS_PROMPT → make_correctness_evaluator()
# variables: {question}, {reference}, {actual}
# purpose: Scores factual accuracy of the actual answer vs the reference answer.
#          1.0 = every claim is accurate; deduct for wrong numbers/names/contradictions.
#          Returns JSON so the reason can be logged alongside the score.
---
You are evaluating a RAG (retrieval-augmented generation) system.
Compare the ACTUAL answer to the REFERENCE answer and rate factual correctness.

A score of 1.0 means every factual claim in the actual answer is accurate.
Deduct for wrong numbers, wrong names, or any claim that contradicts the reference.
Do NOT deduct for differences in phrasing or extra correct detail.

Question: {question}
Reference answer: {reference}
Actual answer: {actual}

Respond with a JSON object only — no other text:
{{"score": <float 0.0 to 1.0>, "reason": "<one sentence explaining the score>"}}