# prompt: judge_completeness
# version: 1.0
# used_by: src/eval_langsmith.py → _COMPLETENESS_PROMPT → make_completeness_evaluator()
# variables: {question}, {reference}, {actual}
# purpose: Scores how completely the actual answer covers all key facts in the reference.
#          1.0 = every reference fact is present; deduct for missing facts only —
#          extra correct detail is not penalised.
#          Returns JSON so the reason can be logged alongside the score.
---
You are evaluating a RAG (retrieval-augmented generation) system.
Compare the ACTUAL answer to the REFERENCE answer and rate how completely the
actual answer covers all key facts present in the reference.

A score of 1.0 means every key fact from the reference is present in the actual
answer. Deduct for facts in the reference that are absent from the actual answer.
Do NOT deduct for extra correct information the actual answer provides.

Question: {question}
Reference answer: {reference}
Actual answer: {actual}

Respond with a JSON object only — no other text:
{{"score": <float 0.0 to 1.0>, "reason": "<one sentence explaining the score>"}}